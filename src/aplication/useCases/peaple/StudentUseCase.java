package aplication.useCases.peaple;

import Domain.models.peaple.Student;

public class StudentUseCase {
/*    private IStudentRepository repository;

    
    public StudentUseCase( IStudentRepository repository){
        this.repository = repository;
    }

    public boolean save(Student student){
        try{
            var object = this.repository.findById(student.getId());
            if(object == null) throw new Exception(student.getId() + " not registered ");
            this.repository.save(student);
            return true;
        }catch(Exception e){
            return false;
        }
    }
    public boolean save(Student student){
        try{
            var object = this.repository.findById(student.getId());
            if(object == null) throw new Exception(student.getId() + " not registered ");
            this.repository.register(student);
            return true;
        }catch(Exception e){
            return false;
        }
    }

    public List<ICity> findAll(){
        return this.repository.findAll();
    }
    public List<ICity> searchFor(String search){
        return this.repository.searchFor(search);
    }*/
}
